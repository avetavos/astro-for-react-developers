---
title: Integrations
description: 'เพิ่ม React, Tailwind, sitemap และอื่นๆ ด้วย npx astro add ทำความเข้าใจ astro.config.mjs และเปรียบเทียบกับ next.config.js'
---

import ReactAstro from '../../../../components/ReactAstro.astro';
import Diff from '../../../../components/Diff.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

ใน Next.js คุณขยาย framework ผ่าน `next.config.js` — webpack plugins, Babel transforms, `rewrites`, `redirects` และ third-party packages ที่ hook เข้า build pipeline Astro มีแนวคิดชั้นหนึ่งที่เรียกว่า **integrations** ที่ครอบคลุม surface area เดียวกัน แต่ชัดเจนกว่า: แต่ละ integration คือ npm package ที่ลงทะเบียนใน `astro.config.mjs` และสามารถแตะ build pipeline, dev server และ output ได้

## `astro.config.mjs` vs `next.config.js`

export const configNext = `// next.config.js
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
});

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['images.unsplash.com'],
  },
  async redirects() {
    return [
      { source: '/old', destination: '/new', permanent: true },
    ];
  },
};

module.exports = withBundleAnalyzer(nextConfig);`;

export const configAstro = `// astro.config.mjs
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import sitemap from '@astrojs/sitemap';
import vercel from '@astrojs/vercel';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://example.com',
  integrations: [
    react(),
    sitemap(),
  ],
  adapter: vercel(),
  vite: {
    plugins: [tailwindcss()],
  },
  redirects: {
    '/old': '/new',
  },
});`;

<ReactAstro react={configNext} astro={configAstro} reactTitle="next.config.js" astroTitle="astro.config.mjs" />

helper `defineConfig` ให้ TypeScript autocomplete เต็มรูปแบบใน editor integration ทั้งหมดอยู่ใน array `integrations: []` — ไม่มี pattern การ wrap/compose แบบ `withBundleAnalyzer(nextConfig)`

## เพิ่ม integrations ด้วย `astro add`

```bash
# UI framework integrations
npx astro add react          # @astrojs/react — enables .tsx islands
npx astro add preact         # @astrojs/preact — lighter React-compatible alternative
npx astro add svelte         # @astrojs/svelte
npx astro add vue            # @astrojs/vue

# Styling
npx astro add tailwind       # installs the @tailwindcss/vite plugin (Tailwind 4)

# Site utilities
npx astro add sitemap        # @astrojs/sitemap — generates sitemap.xml at build
npx astro add mdx            # @astrojs/mdx    — MDX support for content pages

# SSR adapters (pick one)
npx astro add vercel         # @astrojs/vercel
npx astro add netlify        # @astrojs/netlify
npx astro add node           # @astrojs/node (self-hosted Node.js)
npx astro add cloudflare     # @astrojs/cloudflare
```

แต่ละคำสั่งติดตั้ง package **และ** แก้ไข `astro.config.mjs` ให้คุณ คุณสามารถแก้ไข config ด้วยตนเองภายหลังได้เสมอ

## รูปแบบ integration

Astro integration คือเพียง function ที่ return config object นี่คือ custom integration แบบ minimal:

```js
// astro.config.mjs
import { defineConfig } from 'astro/config';

function myPlugin() {
  return {
    name: 'my-plugin',
    hooks: {
      'astro:build:done': ({ dir }) => {
        console.log('Build finished. Output:', dir.pathname);
      },
    },
  };
}

export default defineConfig({
  integrations: [myPlugin()],
});
```

คุณจะแทบไม่ต้องเขียน custom integrations เอง — package `@astrojs/*` อย่างเป็นทางการครอบคลุมกรณีทั่วไปแล้ว — แต่การเข้าใจรูปแบบจะช่วยเมื่อคุณอ่าน source code ของ integration

## ส่ง options ให้ integrations

```js
// astro.config.mjs
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  integrations: [
    react({
      include: ['**/interactive/**'],  // only hydrate components in this dir
    }),
    sitemap({
      filter: (page) => !page.includes('/admin/'),  // exclude private pages
    }),
  ],
});
```

> Tailwind 4 **ไม่ใช่** Astro integration — `npx astro add tailwind` จะติดตั้ง Vite plugin `@tailwindcss/vite` แทน ตั้งค่าไว้ใต้ `vite: { plugins: [...] }` ไม่ใช่ `integrations: [...]` ส่วน package เก่า `@astrojs/tailwind` (ที่มี option `applyBaseStyles`) เลิกใช้แล้ว เกี่ยวข้องกับโปรเจกต์ Tailwind 3 เท่านั้น

<Diff title="next.config.js vs astro.config.mjs">
- Next.js ใช้ CommonJS `module.exports`; Astro ใช้ ESM `export default defineConfig()`
- Next.js wrap plugins ด้วย HOF pattern (`withX(nextConfig)`); Astro ใส่ทุกอย่างใน array `integrations: []`
- `npx astro add` อัตโนมัติทั้ง installation + config; Next.js plugins ต้อง `require()` + wrap ด้วยตนเอง
- ทั้งคู่รองรับ `redirects`, `rewrites` และ environment variable injection ที่ config layer
</Diff>

export const quizIntegrations = [
  {
    q: "วิธีเพิ่ม React integration ในโปรเจกต์ Astro?",
    options: [
      "`npm install react react-dom` เท่านั้น — Astro ตรวจจับอัตโนมัติ",
      "`npx astro add react` — ติดตั้ง package และอัปเดต `astro.config.mjs`",
      "เพิ่ม `jsx: 'react'` ใน `tsconfig.json`",
      "Import React ใน frontmatter ของ `.astro`",
    ],
    answer: 1,
  },
  {
    q: "สิ่งเทียบเท่าของ pattern `withBundleAnalyzer(nextConfig)` ใน Astro คืออะไร?",
    options: [
      "Wrap `defineConfig` ด้วย custom function",
      "เพิ่ม integration ใน array `integrations: []` ใน `defineConfig`",
      "ใช้ `astro.config.ts` แทน `.mjs` เพื่อเปิดใช้ composition",
      "ไม่มีสิ่งเทียบเท่า — Astro ไม่รองรับ plugins",
    ],
    answer: 1,
  },
  {
    q: "`npx astro add tailwind` แก้ไขไฟล์ใดอัตโนมัติ?",
    options: [
      "`tailwind.config.js`",
      "`package.json`",
      "`astro.config.mjs`",
      "`src/styles/global.css`",
    ],
    answer: 2,
  },
];

<Quiz client:visible id="tooling/integrations" questions={quizIntegrations} />

<ProgressTracker client:visible id="tooling/integrations" />
